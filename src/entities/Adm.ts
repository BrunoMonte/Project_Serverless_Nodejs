import { v4 as uuidv4 } from 'uuid';
import { hashSync } from 'bcryptjs';
import md5 from 'md5';

class adm {
    public email: string
    public name: string
    public password: string

    constructor(
        email:string,
        name:string,
        password:string
    ){
        
    }
}